/**
 * Standalone copy of the Socket.IO contract for teams that cannot import
 * `@engine/shared` as a workspace package. Keep in sync with src/types.ts.
 *
 * Prefer importing from `@engine/shared` when you can.
 */
export * from "./src/types.ts";
export * from "./src/phases.ts";
export * from "./src/game-config.ts";
