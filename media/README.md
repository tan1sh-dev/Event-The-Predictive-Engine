# Round media

Drop the live clue assets here. The game already points at these paths:

| File | Round |
|---|---|
| `r1-purchases.png` | R1 — last purchases |
| `r2-vibe.mp3` | R2 — voice note |
| `r3-search.png` | R3 — 1 A.M. searches |
| `r5-tabs.png` | R5 — vision vs CI |
| `final-latent.png` | Final inference — three environments |

Served at `/media/<filename>` by both Vite (dev) and the Express server (prod).
Do not commit volunteer-identifying photos if this repo is public.
