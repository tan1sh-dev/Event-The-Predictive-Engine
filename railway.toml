[build]
builder = "NIXPACKS"
buildCommand = "npm ci --include=dev && npm run build"

[deploy]
startCommand = "npm run start -w @engine/server"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
